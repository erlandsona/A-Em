-- module Caldwell.Update exposing (update)


-- update : Message -> Model -> Model
-- update msg model =
--     case msg of
--         ShowNav True ->
--             log (toString model)
--             { model | showNav = (not showNav) }

--         -- ShowNav False ->
--         --     log (toString model)
--         --     { model | showNav = (not showNav)
--         CurrentPage a ->
--             log (toString )
